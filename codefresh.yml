version: '1.0'

steps:

  unibuild-usync:
    type: build
    description: UC - build step
    image_name: davidva/usync
    dockerfile: Dockerfile-all.txt
    tag: latest
    no_cache: true
#    privileged: true
